# Claude Code (Agentic Loop Prime)

Read [AGENTS.md](AGENTS.md). That file is the boot protocol.

Skills are installed under `.claude/skills/` (slash commands such as `/prime-intake`) and `.agents/skills/` (Resume paths). Same files. Resume names `.agents/`.
